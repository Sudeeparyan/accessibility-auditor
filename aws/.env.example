# ─── AccessibilityAI Auditor — AWS Environment ──────────────────────────────
# Copy this file to .env and fill in your actual keys:
#   cp .env.example .env

# OpenAI API Key (required for LLM-powered semantic analysis)
OPENAI_API_KEY=your-openai-api-key-here

# AWS Credentials (used by Serverless Framework for deployment)
# These are read automatically from ~/.aws/credentials if configured via `aws configure`.
# Only set here if you prefer env-based config:
# AWS_ACCESS_KEY_ID=your-aws-access-key-here
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key-here
# AWS_REGION=us-east-1
